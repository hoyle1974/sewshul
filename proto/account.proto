syntax = "proto3";

package account;
option go_package = "github.com/jstrohm/sewshul/protobuf";

message Account {
  string username = 1;
  string password = 2;
}

service AccountService {
  rpc CreateAccount (Account) returns (AccountResponse);
}

message AccountResponse {
  string message = 1;
}
